# PMtools — เครื่องมือบำรุงรักษาเชิงป้องกัน Windows Server

เก็บสถานะเครื่องแม่ข่ายทุกหัวข้อที่ควรรู้ในรอบเดียว แล้วออกไฟล์รายงาน HTML สองภาษา (ไทย/อังกฤษ)
ที่ส่งให้ผู้บริหารหรือหน่วยงานราชการได้โดยตรง หรือสั่ง Print to PDF แนบหนังสือได้ทันที

## เอกสารทั้งหมด

| ไฟล์ | สำหรับใคร | เนื้อหา |
|---|---|---|
| **[Manual.html](Manual.html)** | **ผู้ใช้งานทั่วไป** | คู่มือฉบับเต็ม เปิดในเบราว์เซอร์ มีสารบัญ ภาพประกอบ พิมพ์เป็น PDF ได้ — **ส่งต่อให้เพื่อนร่วมงานได้เลย** |
| `README.md` (ไฟล์นี้) | ทุกคน | ภาพรวมและวิธีเริ่มต้นแบบย่อ |
| [USAGE.md](USAGE.md) | ผู้ใช้งานที่ต้องการรายละเอียด | เมนู, คำสั่ง PowerShell, กราฟแนวโน้ม CPU/หน่วยความจำ, รหัสสถานะ |
| [DISTRIBUTION.md](DISTRIBUTION.md) | ผู้ดูแลเครื่องมือ | แจกแบบไฟล์เดียว (.cmd/.zip) และส่งออกรายงานเป็น Word |
| [CHECKS.md](CHECKS.md) | ผู้ดูแลเครื่องมือ | หัวข้อที่ตรวจทั้งหมด, การตั้งค่า, วิธีเพิ่มหัวข้อตรวจใหม่ |
| [TECHNICAL.md](TECHNICAL.md) | ผู้พัฒนาต่อ | เหตุผลเชิงเทคนิคเบื้องหลังการตัดสินใจบางจุด (encoding, ภาษา Windows, เครือข่าย) |
| [HANDOVER.md](HANDOVER.md) | ผู้พัฒนาต่อ | สถานะงาน สิ่งที่ค้าง และกฎที่ห้ามผิดเวลาแก้โค้ด |

> **ถ้ากลับมาทำงานต่อหรือจะแก้โค้ด ให้อ่าน `HANDOVER.md` ก่อน**

---

## อ่านค่าอย่างเดียว ไม่แก้ไขระบบ

เครื่องมือนี้ **ไม่เปลี่ยนค่าใด ๆ บนเครื่องแม่ข่าย** ทุกหัวข้อเป็นการอ่านสถานะเท่านั้น
ไม่ตั้งค่า ไม่แก้ registry ไม่เริ่ม/หยุดบริการ ไม่ติดตั้งอะไร และ**ไม่รีสตาร์ทเครื่อง**
สิ่งเดียวที่เขียนลงดิสก์คือโฟลเดอร์รายงานใน `Output\`

มีสองหัวข้อเท่านั้นที่ติดต่อออกนอกเครื่อง (`WU` ถาม WSUS/Microsoft Update, `CONN` ส่ง ping)
ทั้งสอง**ปิดไว้เป็นค่าเริ่มต้น** เปิดใช้เฉพาะครั้งได้ด้วย `-Only WU` หรือ `-Only CONN`
รายละเอียดครบอยู่ใน [CHECKS.md](CHECKS.md)

---

## เริ่มใช้งาน

1. คัดลอกโฟลเดอร์ `PMtools` ทั้งโฟลเดอร์ไปวางบนเครื่องแม่ข่าย (หรือใช้[ไฟล์เดียวแบบ .cmd/.zip](DISTRIBUTION.md))
2. เปิด `Config\settings.json` แก้ชื่อหน่วยงานและชื่อผู้ตรวจสอบ (ทำครั้งเดียว)
3. ดับเบิลคลิก **`Run-PM.cmd`** แล้วกดอนุญาตเมื่อระบบขอสิทธิ์ผู้ดูแลระบบ
4. เลือกจากเมนูว่าจะเก็บกราฟ CPU/หน่วยความจำก่อนกี่นาที (กด Enter เฉย ๆ = ตรวจทันที)
5. รายงานจะเปิดขึ้นเองในเบราว์เซอร์ กด `Ctrl + P` เลือก **Save as PDF** เพื่อได้ไฟล์แนบหนังสือ

หรือสั่งตรงจาก PowerShell:

```powershell
.\Start-PMCheck.ps1 -OpenReport
```

ขั้นตอนละเอียด รวมถึงหน้าตาเมนูและสวิตช์อื่น ๆ อยู่ใน **[USAGE.md](USAGE.md)**

ผลลัพธ์อยู่ที่ `Output\<ชื่อเครื่อง>_<วันเวลา>\PM-Report.html` (คู่กับ `PM-Data.json` ที่เก็บข้อมูลดิบไว้ครบ)

---

## รหัสสถานะเมื่อรันจบ

`0` ปกติ &nbsp;&nbsp; `1` พบประเด็นที่ควรเฝ้าระวัง &nbsp;&nbsp; `2` พบประเด็นวิกฤตหรือตรวจไม่สำเร็จ
— ใช้ต่อยอดกับ Scheduled Task ได้ รายละเอียดใน [USAGE.md](USAGE.md)
